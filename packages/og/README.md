# @0xagentio/og

0G Storage and KV adapters for AgentIO.

This package provides `ogStorage(...)`, `ogKvObjectClient(...)`, file-object helpers, memory test clients, deterministic codecs, key helpers, and 0G progress logging utilities.

Use it when an AgentIO runtime should persist mutable agent state and audit events through 0G-compatible object storage.
