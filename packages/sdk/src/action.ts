import type { ActionIntent } from '@0xagentio/core';

/**
 * Creates a generic action intent for examples and application code.
 */
export function createActionIntent(action: ActionIntent): ActionIntent {
  return action;
}
