# @0xagentio/noir

Noir proof adapter for AgentIO authorization proofs.

This package provides `noirProofs(...)`, proof payload encoding helpers, compiled circuit artifact loading, and witness input helpers for the AgentIO authorization circuit.

The package includes the compiled authorization circuit artifact under `circuits/authorization/target/`. Run `npm run noir:compile` in the repository when developing the circuit itself.
